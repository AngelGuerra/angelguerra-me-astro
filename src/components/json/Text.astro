---
export interface Props {
  title: string;
  last?: boolean;
}

const { title, last = false } = Astro.props;
---

<span class="block ml-4">
  <span class="text-rose-800">"{title}"</span>: <span class="text-cyan-800">"<slot />"</span>{!last && ","}
</span>